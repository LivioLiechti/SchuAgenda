spring:
  application:
    name: dbagenda
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dbagenda?serverTimezone=UTC
    username: root
    password: $F3r13n$
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
checkBenutzerEinsatz:
  delay: 0 0 6 * * *